<h1>Products</h1>

<%= link_to "New Product", new_product_path %>

<div class="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
